# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PopularArticles' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PopularArticles
  pod 'SDWebImage', '~> 5.0'
  
  target 'PopularArticlesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PopularArticlesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
